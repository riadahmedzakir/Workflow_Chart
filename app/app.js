(function (angular) {
    "use strict";

    var module = angular.module("app", ['ngMaterial']);
    function config() {

    }
    config.$inject = [];
    module.config(config);

    function run() {
    }
    run.$inject = [];
    module.run(run);
})(window.angular);